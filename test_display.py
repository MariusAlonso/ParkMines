from display import *
from inputs import *
from parking import *

class TestTest():

    def test_display_parking(self):
        """
        Vehicle.next_id = 1
        stock = Stock(import_from_file()[:20])
        parking = Parking([Block([Lane(1, 10), Lane(2, 10)]), Block([Lane(1, 10)]), Block([Lane(1, 10)])], [[0,0],[1,2]], (0,0))
        display = Display(datetime.datetime(2016,1,1,0,0,0,0), stock, 1, parking, AlgorithmRandom, 15, 20)
        """
        assert 0 == 0